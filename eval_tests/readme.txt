To test all the test files:
Edit SRCS in run_all.sh to match the path to the test files. Then run the command:
bash -x run_all.sh

Ctrl + Z terminates the whole testing, Ctrl + C terminates just the ongoing command.
